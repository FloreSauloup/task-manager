<h1>Louis buddy des Arts :D</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
    <%= task.completed ? "✅" : "❌" %>
    <%= link_to task.title, task_path(task.id)%>
    </li>
  <% end %>
</ul>

<p>
<%= link_to "Create Task", new_task_path%>
</p>
